---
title: Connecting to Your Database
description: Add Neo4j and PostgreSQL connections to NeoBoard.
sidebar:
  order: 4
---

NeoBoard supports two database types: **Neo4j** (graph) and **PostgreSQL** (relational). This guide shows you how to add and test connections for both.

![Connections page](/screenshots/connections.png)

## Adding a Neo4j Connection

1. Navigate to **Connections** in the sidebar.
2. Click **Add Connection**.
3. Select **Neo4j** as the connection type.
4. Fill in the connection details:

| Field | Example | Notes |
|-------|---------|-------|
| **Name** | Movie Database | A label to identify this connection |
| **URI** | `bolt://localhost:7687` | Bolt protocol URI |
| **Username** | `neo4j` | Your Neo4j username |
| **Password** | `password` | Your Neo4j password |
| **Database** | `neo4j` | Optional. Defaults to the default database |

Timeouts are optional and live under **Advanced Settings**, in milliseconds (1000–300000). Neo4j has Connection, Query and Acquisition timeouts. Queries time out after 30000 ms (30 s) unless you set Query Timeout. For TLS, use a `neo4j+s://` or `bolt+s://` URI.

5. Click **Test Connection** to verify NeoBoard can reach your database.
6. Click **Create**. The test is optional: NeoBoard saves the connection either way, tests it right after saving, and shows an **Error** badge if it cannot connect.

## Adding a PostgreSQL Connection

1. Navigate to **Connections** in the sidebar.
2. Click **Add Connection**.
3. Select **PostgreSQL** as the connection type.
4. Fill in the connection details:

| Field | Example | Notes |
|-------|---------|-------|
| **Name** | Analytics DB | A label to identify this connection |
| **URI** | `postgresql://localhost:5432/mydb` | Host, port and optional database. Do not include a username or password; the URI is rejected if it contains a password |
| **Username** | `postgres` | Your PostgreSQL user (required) |
| **Password** | `password` | Your PostgreSQL password (required) |
| **Database** | `mydb` | Optional. Used only when the URI has no database path; defaults to `postgres` |

Timeouts are optional and live under **Advanced Settings**, in milliseconds (1000–300000). PostgreSQL has Connection, Idle and Statement timeouts. Queries time out after 30000 ms (30 s) unless you set Statement Timeout.

For TLS, add `?sslmode=require` to the URI to encrypt, or `?sslmode=verify-full` to encrypt and verify the certificate. Turning on **Reject Unauthorized SSL** under Advanced Settings also forces TLS with certificate verification, and it overrides the URI's `sslmode`.

5. Optionally click **Test Connection** to verify, then click **Create**. NeoBoard saves the connection either way and shows an **Error** badge if it cannot connect.

## Troubleshooting

If you see an **Error** badge next to your connection, try these steps.

### Check Your URI and Credentials

Make sure the URI, username, and password are correct. A typo in any field will cause a connection failure.

### Docker Networking

If NeoBoard and your database both run in Docker, use the **container name** instead of `localhost`:

```
bolt://neoboard-neo4j:7687
postgresql://neoboard-postgres:5432/mydb
```

Containers on the same Docker network communicate by service name, not `localhost`.

### Local Databases

If your database runs directly on your machine (not in Docker), use `localhost` with the correct port:

- Neo4j Bolt: `7687` (default)
- PostgreSQL: `5432` (default)

### Firewall and Network

Ensure no firewall rules block traffic between NeoBoard and your database. Cloud-hosted databases may require you to allowlist the NeoBoard server IP.

## Next Steps

- Use your new connection to [create your first dashboard](/start-here/first-dashboard).
- Explore all available [widget types](/using/widgets) including charts, tables, graphs, and forms.
- See [Connectors](/using/connectors) for every advanced option each database type accepts.
- No database to hand? [Tour NeoBoard with demo data](/start-here/tour) seeds one for you.
