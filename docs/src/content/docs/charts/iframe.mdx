---
title: iFrame Widget
description: Embed an external web page within a dashboard widget.
sidebar:
  order: 11
---

## Overview

The iFrame widget embeds an external web page directly into a dashboard. Use it to include third-party tools, documentation pages, or external dashboards alongside your NeoBoard widgets. No query or database connection is required.

## Query Format

This widget does not use a query. Configure the URL and settings in the Chart Options panel.

## Options

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| URL | text | (empty) | The URL of the external page to embed. Must be an https:// URL |
| Title | text | Embedded content | Accessible title for screen readers |
| Sandbox Policy | text | (empty) | HTML sandbox attributes controlling what the embedded page can do |

## Sandbox Policy

The sandbox field accepts standard HTML `sandbox` attribute values. Common values:

| Value | Effect |
|-------|--------|
| (cleared) | No tokens: no scripts, no forms, no popups. New widgets start here |
| (never set) | A stored widget with no sandbox value runs as `allow-scripts allow-popups`, even though the field shows empty |
| `allow-scripts` | Allow JavaScript execution |
| `allow-forms` | Allow form submission |
| `allow-popups` | Allow the embedded page to open new windows |

Tokens outside NeoBoard's allow-list are removed before the page is embedded, and the editor warns under the field when that happens. `allow-same-origin` is always refused: combined with `allow-scripts` it would let the embedded page remove its own sandbox. `allow-top-navigation` is refused too; use `allow-top-navigation-by-user-activation` instead.

## Tips

- Always use `https://` URLs. Most browsers block mixed content (HTTP inside HTTPS).
- For untrusted content, type a restrictive value such as `allow-forms` instead of relying on an empty field. An empty field is fully sandboxed only if it was saved empty; a widget that never stored a value allows scripts and popups.
- Some sites block embedding via `X-Frame-Options` or `Content-Security-Policy` headers -- if the page appears blank, the target site may not allow iframing.
