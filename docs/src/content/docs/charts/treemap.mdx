---
title: Treemap
description: Hierarchical data as nested rectangles sized by value.
---

## Overview

The Treemap displays hierarchical data as nested rectangles where each tile's area is proportional to its value. It shares the same data format as Sunburst and Circle Packing — you can switch between them freely.

## Query Format

Return hierarchical data with `name` and `value` columns. A `parent` column turns flat rows into a tree.

The contract, in full:

- **Leaf rows are enough.** A name that appears only in the `parent` column is created for you — you do not need to return a row for each parent.
- **A parent's own value is the sum of its children**, unless you return a larger one explicitly. A parent value smaller than its children's total is ignored, because the children would overflow it.
- **Rows sharing a parent and a name are summed** into one node. The same name under two different parents stays two nodes.
- **Values must be positive numbers.** A row whose value is zero, negative, missing or non-numeric is skipped — it has no area to draw. Numeric strings are accepted.
- **Every column you return travels with the node**, so styling rules and click actions can use columns beyond `name` and `value`.
- If the `parent` links form a cycle, the widget says so instead of drawing a partial tree.

### Cypher

```cypher
MATCH (c:Category)-[:CONTAINS]->(p:Product)
RETURN c.name AS parent, p.name AS name, p.sales AS value
```

### SQL

```sql
SELECT category AS parent, product_name AS name, revenue AS value
FROM products
ORDER BY category, revenue DESC
```

## Options

| Option | Default | Description |
|--------|---------|-------------|
| Show Labels | `true` | Show tile name labels |
| Show Values | `false` | Add the value after the name on each tile |

## Features

- **Nested tiles** — area proportional to value
- **One hue per group** — the first three groups take palette colours and their children inherit them; further groups share one quiet fill, and the group label identifies them. Flat data is a single hue, because area already carries the value and a colour per tile would encode nothing.
- **Drill-down** — click a tile to zoom into its children. The breadcrumb appears only when the data has depth to drill into.
- **Tooltip** — the value, then the path from the top-level group down to the tile
- **Rule-based styling** — color tiles by condition
- **Click actions** — click a tile to navigate or set a parameter. Configuring one turns off drill-down, so a single click does one thing.
