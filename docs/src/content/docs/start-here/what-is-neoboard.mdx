---
title: What is NeoBoard
description: An open-source dashboarding tool for Neo4j and PostgreSQL, and a map of this documentation.
sidebar:
  order: 1
---

NeoBoard is an open-source dashboarding tool for hybrid database architectures. It connects to **Neo4j** and **PostgreSQL** databases, letting you build rich, interactive dashboards without writing frontend code.

**Key capabilities:**

- **16 chart types** -- bar, line, pie, single value, table, gauge, sankey, sunburst, gantt, graph visualization, map, JSON viewer, form, markdown, iframe, and parameter select
- **Interactive parameters** -- dropdowns, date pickers, sliders, and text fields that filter data across all widgets
- **Form widgets** -- run write queries (INSERT, CREATE, UPDATE) directly from the dashboard
- **Multi-page dashboards** -- organize related views into pages with drag-and-drop layouts
- **Export and import** -- share dashboards as JSON files across NeoBoard instances
- **Role-based access** -- Admin, Creator, and Reader roles with granular write permissions
- **Encrypted credentials** -- AES-256-GCM encryption for all stored database passwords
- **Multi-tenancy** -- full tenant isolation for SaaS deployments

## Where to go next

- **Start here** -- [install](/start-here/install) NeoBoard, [tour it with demo data](/start-here/tour), then build [your first dashboard](/start-here/first-dashboard). Coming from NeoDash? See [Migrating from NeoDash](/start-here/migration-from-neodash). Stuck? [Troubleshooting](/start-here/troubleshooting).
- **Using NeoBoard** -- how [dashboards](/using/dashboards), [widgets](/using/widgets) and [parameters](/using/parameters) fit together, and how to [connect a database](/using/connecting-databases).
- **Chart types** -- the [chart reference](/charts), one page per type with the query shape it expects.
- **Deploy and operate** -- [production deployment](/deploy/production) with Docker Compose or [from a build](/deploy/run-from-a-build), [configuration](/deploy/configuration), [backups](/deploy/backup-restore) and [monitoring](/deploy/monitoring).
- **Security and access** -- [password login](/security/password-login), [roles](/security/roles), [API keys](/security/api-keys) and [credential encryption](/security/credential-encryption).
- **CLI** -- the [`neoboard` command](/cli) that installs, starts and seeds everything.
- **Extend and contribute** -- the [architecture](/extend/architecture), [chart](/extend/new-chart-plugin) and [connector](/extend/new-connector-plugin) plugins, and the [development setup](/extend/setup).
