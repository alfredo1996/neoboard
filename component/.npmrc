## @neo4j-cypher/react-codemirror declares react ^18 as a peer dep,
## but we only import the `cypher()` language extension (pure CM6, no React).
## The CypherEditor React component is NOT used. Safe to ignore mismatch.
legacy-peer-deps=true
