/**
 * Dashboard edit mode.
 *
 * The UI is rendered by `[id]/layout.tsx`, which reads the mode off the URL
 * segment and stays mounted across the /[id] ↔ /[id]/edit navigation. This
 * segment exists only to make the URL routable (#1370).
 */
export default function DashboardEditorPage() {
  return null;
}
